<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Cor de fundo do EditText -->
    <corners android:radius="40dp"/> <!-- Define o raio das bordas -->
    <stroke
        android:width="2dp"
        android:color="@android:color/black"/> <!-- Define a cor e largura da borda -->
</shape>